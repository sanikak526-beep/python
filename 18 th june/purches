class dmart:
    storename = "DMart"
    def __init__(self, category, product_name, price, quantity):
        self.category = category
        self.product_name = product_name
        self.price = price
        self.quantity = quantity

        @classmethod
        def display_store_details(cls):
            return f"store name -->{cls.storename}"
        def common_features(self):
            return f"category: {self.category}\nproduct_name:{self.product_name}\n quantity:{self.quantity}\nprice:{self.price}"




            from dmart import dmart
class purches(dmart):
    def __init__(self, category, product_name, price, quantity):
        self.category = category
        self.product_name = product_name
        self.price = price
        self.quantity = quantity

    
    
    def display_purches_details(self):
        print(super().display_store_details())
        print(super().common_features())
        return f"product_name:{self.product_name}\nprice:{self.price}\nquantity:{self.quantity}"
